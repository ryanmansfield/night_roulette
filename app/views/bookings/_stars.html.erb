<% 5.times do |count|%>
    <% if @venue.rating > (count + 0.6)%>
    <i class="fas fa-star"></i>
    <% else %>
    <i class="fas fa-star" style="opacity:.3;"></i>
    <% end %>
<% end %>
<p class="rating-counter">(<%= @venue.times_rated %> ratings)</p>
